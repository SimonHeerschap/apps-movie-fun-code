applications:
- name: apps-movie-fun-code
  instances: 1
  memory: 1024M
  disk_quota: 1024M
  random-route: true
  stack: cflinuxfs2
  path: target/moviefun.war
  services:
    - attendee-mysql
  buildpack: java_buildpack_offline
